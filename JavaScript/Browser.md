# Browser

## Types of Connections

There are various types of connections one can use:

* HTTP request-response cycle;
* AJAX polling;
* Websockets (bidirectional communication);
* Server-sent events (unidirectional communication).
